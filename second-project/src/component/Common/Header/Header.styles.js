import styled from "styled-components";

export const StyledHeader = styled.header`
  width: 100%;
  height: 200px;
  background: linear-gradient(
    135deg,
    rgb(251, 110, 110),
    rgba(218, 119, 227, 0.3)
  );
  display: flex;
  justify-content: center;
  align-items: center;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  font-size: 2rem;
  font-weight: 600;
  color: white;
  position: relative;
`;
